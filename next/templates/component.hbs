import styles from './{{name}}.module.scss';
import type { {{name}}Types } from './{{name}}.types';

export default function {{pascalCase name}}({  }: {{name}}Types) {
  return (
    <section className={styles['{{pascalCase name}}']}>

    </section>
  );
}
