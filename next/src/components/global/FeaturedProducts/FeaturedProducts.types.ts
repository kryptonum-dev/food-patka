import type { ProductCardTypes } from '@/components/global/ProductCard/ProductCard.types';

export type FeaturedProductsTypes = {
  heading: string;
  paragraph: string;
  list: ProductCardTypes[];
};
